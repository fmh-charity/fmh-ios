
import UIKit

// MARK: - General colors

public struct Color {
    static let accent = UIColor(red: 0, green: 0.65, blue: 0.64, alpha: 1)
}

// MARK: - Titles

public extension Color {
    
    enum title {
       
    }
}

// MARK: - Controls

public extension Color {
    
    enum button {
        
    }
}
